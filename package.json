{
  "name": "hold-still-extension",
  "version": "1.1.11",
  "private": true,
  "description": "Local-first full-page, viewport, and selected-area screenshot extension.",
  "scripts": {
    "test": "node tests/smoke.js"
  }
}
